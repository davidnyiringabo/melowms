'use client';
import { Link } from 'react-router-dom';

const AuthNav = () => {
  return (
    <div className="flex items-center gap-2">
      {/* <Link
        className="font-bold uppercase p-2 border-b border-melo-200 hover:bg-melo-100"
        to={'/'}
      >
        Home
      </Link>
      <Link
        className={`font-bold uppercase p-2 border-b border-melo-200`}
        to={'/auth/login'}
      >
        Login
      </Link> */}
    </div>
  );
};

export default AuthNav;
