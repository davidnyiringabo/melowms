// CREATE TABLE "INVENTORY"."PURCHASEEBM" ("ID" INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), "ORGININVO" INTEGER DEFAULT 0, "PURCHGRPID" VARCHAR(100) NOT NULL, "TOTITEMCOUNT" INTEGER NOT NULL, "CFMDATE" VARCHAR(200) NOT NULL, "PYMTTYPCODE" VARCHAR(200) NOT NULL, "REPTTYPCODE" VARCHAR(200) NOT NULL, "REMARK" VARCHAR(500), "PURCHASEDATE" VARCHAR(20), "WAREHSEDATE" VARCHAR(20), "STCKRLSDATE" VARCHAR(30), "CNCELRQSTDTE" VARCHAR(20), "CNCELDATE" VARCHAR(20), "REFUNDDTE" VARCHAR(20), "SPPLIERBRNCHID" VARCHAR(20), "SPPLIERNM" VARCHAR(200), "SPPLIERTIN" VARCHAR(20), "SPPLYINVNBR" INTEGER, "SUPPLYSDCID" VARCHAR(100), "REGISTRTYPCD" VARCHAR(10), "PURCHASETYPCD" VARCHAR(10), "PURCHASESTTSCD" VARCHAR(10), "TAXABLEAMTA" DOUBLE NOT NULL, "TAXABLEAMTB" DOUBLE NOT NULL, "TAXABLEAMTC" DOUBLE NOT NULL, "TAXABLEAMTD" DOUBLE NOT NULL, "TAXRTA" DOUBLE NOT NULL, "TAXRTB" DOUBLE NOT NULL, "TAXRTC" DOUBLE NOT NULL, "TAXRTD" DOUBLE NOT NULL, "TAXAMTA" DOUBLE NOT NULL, "TAXAMTB" DOUBLE NOT NULL, "TAXAMTC" DOUBLE NOT NULL, "TAXAMTD" DOUBLE NOT NULL, "TOTTAXABLEAMT" DOUBLE NOT NULL, "TOTTAXAMT" DOUBLE NOT NULL, "TOTAMT" DOUBLE NOT NULL, "DEBIT" DOUBLE DEFAULT 0, "DEBITPAY" DOUBLE DEFAULT 0, "DATEE" VARCHAR(30) NOT NULL, "PCHSESENT" VARCHAR(2) NOT NULL DEFAULT 'N', "STOCKEDYN" VARCHAR(2) NOT NULL DEFAULT 'N');

import { CodeArray } from './itemCodesData';
    
export const purchaseReceiptType: CodeArray = [
  { code: 'P', codeName: 'Purchase' },
  { code: 'R', codeName: 'Refund After Purchase' },
];

export const registrationTypeCode: CodeArray = [
  { code: 'A', codeName: 'Automatic' },
  { code: 'M', codeName: 'Manual' },
];

export const purchaseStatusCode: CodeArray = [
  { code: '01', codeName: 'Wait for Approval' },
  { code: '02', codeName: 'Approved' },
  { code: '03', codeName: 'Cancel Requested' },
  { code: '05', codeName: 'Canceled' },
  { code: '05', codeName: 'Refunded' },
  { code: '06', codeName: 'Transferred' },
];

// CREATE TABLE "INVENTORY"."PURCHASEEBMITEM" ("ID" INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), "PURCHID" INTEGER NOT NULL, "PURCHGRPID" VARCHAR(100) NOT NULL, "ITEMID" INTEGER NOT NULL DEFAULT 0, "ITEMSEQ" INTEGER NOT NULL DEFAULT 0, "SPLYITEMCLSCODE" VARCHAR(100), "SPLYITEMCODE" VARCHAR(100) NOT NULL, "SPLYITEMNAME" VARCHAR(200) NOT NULL, "SPLYBARCODE" VARCHAR(30), "PKGUNITYCODE" VARCHAR(10) NOT NULL, "PACKAGEPKG" DOUBLE NOT NULL DEFAULT 1, "QTYUNITYCODE" VARCHAR(10) NOT NULL, "QTY" DOUBLE NOT NULL DEFAULT 0, "MYQTY" DOUBLE DEFAULT 0, "REMAINMYQTY" DOUBLE DEFAULT 0, "UCOST" DOUBLE NOT NULL DEFAULT 0, "MYUCOST" DOUBLE DEFAULT 0, "SPLYAMT" DOUBLE NOT NULL DEFAULT 0, "DISCRT" DOUBLE NOT NULL DEFAULT 0, "DISCAMT" DOUBLE NOT NULL DEFAULT 0, "TAXTYPCODE" VARCHAR(5), "TAXABLEAMT" DOUBLE NOT NULL DEFAULT 0, "TAXAMT" DOUBLE NOT NULL DEFAULT 0, "TOTAMT" DOUBLE NOT NULL DEFAULT 0, "BATCHNO" VARCHAR(200), "EXPIRATION" VARCHAR(200), "DATEE" VARCHAR(20) NOT NULL, "STOCKSENT" VARCHAR(2) DEFAULT 'N', "MINQTY" INTEGER NOT NULL DEFAULT 0, "UPRICE" DOUBLE NOT NULL DEFAULT 0, "MINPRICE" DOUBLE NOT NULL DEFAULT 0);